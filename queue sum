#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a[5]={8,2,3,4,5};
    queue<int> q;
    for(int i=0;i<5;i++)
    {
        q.push(a[i]);
    }
    int sum=0;
    while(!q.empty())
    {
        sum+=q.front();
        q.pop();
    }
    cout<<sum;
    
}
